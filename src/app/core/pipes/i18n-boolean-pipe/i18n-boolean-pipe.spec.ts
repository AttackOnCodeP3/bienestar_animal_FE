import { I18nBooleanPipe } from './i18n-boolean-pipe';

describe('I18nBooleanPipe', () => {
  it('create an instance', () => {
    const pipe = new I18nBooleanPipe();
    expect(pipe).toBeTruthy();
  });
});
