/**
 * User Management Displayed Columns Enum.
 * This enum defines the columns that will be displayed in the user management table angular material table.
 * Each enum value corresponds to potential column names in the table.
 * @author dgutierrez
 */
export enum UserManagementDisplayedColumnsTableEnum {
  NAME = 'name',
  LASTNAME = 'lastname',
  EMAIL = 'email',
  ACTIVE = 'active',
  BLOCK = 'block',
  ACTIONS = 'actions',
}
