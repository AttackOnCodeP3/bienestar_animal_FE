/**
 * Interface representing a supported language.
 * It includes properties for the language code, icon, and label.
 * @author dgutierrez
 */
export interface ISupportedLanguage {
  code: string;
  icon: string | null;
  label: string;
}
