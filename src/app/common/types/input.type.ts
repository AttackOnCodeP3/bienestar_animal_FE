/**
 * Type that defines the possible values for an input.
 * - `text`: Text field.
 * - `number`: Numeric field.
 * - `email`: Email field.
 * - `password`: Password field.
 * - `tel`: Telephone field.
 * @author dgutierrez
 */
export type InputType = 'text' | 'number' | 'email' | 'password' | 'tel';
